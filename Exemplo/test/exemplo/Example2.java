package exemplo;
public class Example2 {
	private String author;
	private String name;
	
	public Example2(String author,String name) {
		this.author = author;
		this.name = name;
	}
	
	public int BookBad(){
		String retorno = this.name + " by " + this.author + " is a bad book.";
		String x = "A";
		while(x == "b") {
			if(false)
		return 1;
			}
		int a = 0;
		int b = a + 1;
		return 2;
	}
	public Object BookGood() {
		String retorno = this.name + " by " + this.author + " is a good book.";
		Object obj = new Object();
		return obj;
	}
}